---
phase: 09-evals-fixtures
plan: "01"
type: execute
wave: 1
depends_on: ["04-01", "07-01", "08-01"]
files_modified:
  - evals/design-engineering/eval.yaml
  - evals/design-engineering/tasks/
autonomous: true
requirements: ["EVAL-01"]
user_setup: []
must_haves:
  truths:
    - "Routing tests cover must-trigger and must-not-trigger boundaries"
    - "Behavior tests cover reuse-first, degradation, rendered evidence and human merge policy"
    - "Model-backed evals remain on trusted execution paths and PR checks remain cheap"
  artifacts:
    - path: "evals/design-engineering/eval.yaml"
      provides: "Behavioral regression suite for the design-engineering capability"
    - path: "evals/design-engineering/tasks/"
      provides: "Deterministic routing and behavior task fixtures"
  key_links:
    - from: "evals/design-engineering/eval.yaml"
      to: "plugins/design-engineering/skills/design-engineering/SKILL.md"
      via: "catalog eval resolver maps suite skill name to the unique capability skill"
---
<objective>
Turn the already-implemented core design workflow's most important routing and safety judgments into regression evidence before the first real pilot.

This phase no longer waits for Phases 5/6: their external specialist enrollment is blocked by Phase 1/#46, while EVAL-01 itself targets the local orchestrator, DESIGN.md/browser behavior and optional-integration degradation that already exist. Phases 5/6 must extend this suite with specialist-specific cases when they enroll their components. Phase 10 still waits for 5/6 as well as this phase, so the real-app pilot cannot bypass the supply-chain gate.
</objective>
<context>
@.planning/REQUIREMENTS.md
@evals/
@plugins/design-engineering/skills/design-engineering/SKILL.md
@plugins/design-engineering/skills/design-engineering/references/workflow.md
@plugins/design-engineering/skills/design-engineering/references/design-md.md
@plugins/design-engineering/skills/design-engineering/references/browser-loop.md
@.github/workflows/eval-static.yml
@.github/workflows/eval-behavioral.yml
@scripts/validate-evals.py
</context>
<tasks>
<task type="auto">
  <name>Create deterministic routing and behavior task fixtures</name>
  <files>evals/design-engineering/tasks/</files>
  <read_first>existing repository Waza tasks and scripts/validate-evals.py</read_first>
  <action>Create compact task YAML fixtures covering broad improve-interface activation, high-impact prototype mode, read-only audit mode, existing-library reuse, scoped DESIGN.md precedence, missing optional Figma/browser degradation, rendered browser evidence, human visual merge boundary, mechanical frontend non-trigger, and backend-only non-trigger. Keep scenarios credential-free and encode context directly in prompts rather than adding a second fixture harness that Waza does not consume.</action>
  <verify>Every task has one correctly configured trigger grader; every positive task also has a behavioral prompt grader; both positive and negative cases exist.</verify>
  <acceptance_criteria>Fixtures test routing and behavioral invariants, not subjective aesthetic scores.</acceptance_criteria>
  <done>Representative task fixtures exist using the repository's established Waza format.</done>
</task>
<task type="auto">
  <name>Add the design-engineering behavioral suite</name>
  <files>evals/design-engineering/eval.yaml</files>
  <read_first>evals/planning/eval.yaml, evals/backlog-management/eval.yaml</read_first>
  <action>Add a schemaVersion 1.2 catalog-owned suite targeting the unique design-engineering capability skill, following existing copilot-sdk/trial/metric conventions and loading tasks/*.yaml.</action>
  <verify>python scripts/validate-evals.py succeeds and Waza static spec verification resolves plugins/design-engineering/skills/design-engineering unambiguously.</verify>
  <acceptance_criteria>No stochastic global visual-quality score is introduced.</acceptance_criteria>
  <done>Core design behavior is regression-specified.</done>
</task>
<task type="auto">
  <name>Verify existing static and trusted execution gates cover the capability</name>
  <files>evals/design-engineering/eval.yaml</files>
  <read_first>.github/workflows/eval-static.yml, .github/workflows/eval-behavioral.yml, scripts/validate-evals.py</read_first>
  <action>Confirm the generic PR path filters already include evals and capability skill/config changes, static validation remains secret-free, and model-backed execution remains workflow_dispatch/scheduled trusted-only. Do not modify CI when the generic contract already satisfies EVAL-01. Record the evidence in the phase summary.</action>
  <verify>The Phase 9 PR runs Behavioral Eval Static Validation; trusted behavioral workflow remains non-PR and credential-gated.</verify>
  <acceptance_criteria>Future external component/package changes cannot silently bypass static eval validation, while untrusted PRs receive no model credentials.</acceptance_criteria>
  <done>EVAL-01 is ready to complete once static CI is green; Phases 5/6 carry an explicit follow-up to add specialist-specific cases at enrollment time.</done>
</task>
</tasks>
