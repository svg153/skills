---
phase: 01-apm-renovate-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dependencies/external-skills/apm.yml
  - dependencies/external-skills/apm.lock.yaml
  - skills/github-build-or-reuse/metadata.yaml
  - renovate.json
autonomous: false
requirements: ["SUPPLY-01"]
user_setup:
  - step: "Hosted Renovate GitHub App is installed/authorized for svg153/skills and a genuine stable upstream release newer than the current pin exists."
    why: "The proof must use a real Renovate update, not a fabricated release."
must_haves:
  truths:
    - "A genuine upstream release produces a reviewed Renovate APM dependency PR"
    - "Trusted completion locks, audits, materializes and validates the exact immutable dependency"
    - "Legacy sync ownership is cut over only after proven parity and deterministic rollback"
  artifacts:
    - path: "dependencies/external-skills/apm.lock.yaml"
      provides: "Immutable resolved dependency and integrity evidence"
    - path: "skills/github-build-or-reuse/metadata.yaml"
      provides: "Explicit sync ownership after successful cutover"
  key_links:
    - from: "dependencies/external-skills/apm.yml"
      to: "dependencies/external-skills/apm.lock.yaml"
      via: "APM resolution of Renovate-proposed immutable dependency"
---
<objective>
Complete the operational proof tracked by #46 before this initiative adds further external design skills.

Purpose: make the existing APM/Renovate supply-chain path proven rather than theoretical.
Output: genuine update evidence, deterministic rollback, and explicit sync cutover ownership.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@dependencies/external-skills/apm.yml
@dependencies/external-skills/apm-policy.yml
@renovate.json
</context>

<tasks>
<task type="checkpoint:human-action">
  <name>Wait for and identify a genuine Renovate APM update</name>
  <action>Use #46's contract exactly. Do not fabricate an upstream release. Confirm hosted Renovate processes the committed configuration and opens the real update PR.</action>
  <verify>The PR is Renovate-authored, targets main, changes only the expected dependency proposal scope, and corresponds to a genuine stable upstream release.</verify>
  <done>A valid real update PR exists and is ready for trusted completion.</done>
</task>
<task type="auto">
  <name>Run trusted APM completion and evidence gates</name>
  <files>dependencies/external-skills/apm.yml, dependencies/external-skills/apm.lock.yaml, skills/github-build-or-reuse/SKILL.md</files>
  <read_first>issue #44, issue #46, trusted APM completion workflow/scripts</read_first>
  <action>Refresh lock through the repository-owned trusted path, run policy/integrity audit and SBOM evidence, clean transient APM cache, materialize the exact locked upstream payload, regenerate distribution, and run catalog/routing/behavioral validation. Preserve metadata ownership.</action>
  <verify>All trusted completion checks pass and the lock still records resolver-only/no duplicate runtime deployment semantics.</verify>
  <acceptance_criteria>No secret, unrelated file, metadata overwrite, or untrusted PR content enters the trusted path.</acceptance_criteria>
  <done>The genuine dependency update is fully materialized and validated in the PR.</done>
</task>
<task type="auto">
  <name>Prove rollback and cut over sync ownership</name>
  <files>skills/github-build-or-reuse/metadata.yaml, renovate.json</files>
  <read_first>issue #44 ownership/cutover rules, current mirror metadata</read_first>
  <action>Demonstrate deterministic return to the prior immutable release, then restore the new release. Only after parity succeeds, set APM as the mirror's synchronization owner and verify the legacy scheduled sync skips this mirror while continuing other eligible entries.</action>
  <verify>Rollback converges byte-for-byte for dependency state/mirror/generated distribution; legacy sync explicitly skips only the APM-managed mirror.</verify>
  <acceptance_criteria>#46's real-update and cutover rows can be truthfully checked off.</acceptance_criteria>
  <done>SUPPLY-01 is complete and later phases may add governed external skills.</done>
</task>
</tasks>
