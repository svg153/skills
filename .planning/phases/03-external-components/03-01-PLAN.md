---
phase: 03-external-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/design-engineering/distribution.config.json
  - scripts/generate-capability-plugin.py
  - skills/skill-publish/scripts/catalog_capability.py
  - tests/test_capability_plugins.py
  - tests/test_skill_publish_capability.py
  - dependencies/external-skills/apm-policy.yml
autonomous: true
requirements: ["EXT-01"]
user_setup: []
must_haves:
  truths:
    - "A capability can declare external skill components resolved from committed APM lock state"
    - "Materialized plugin copies are deterministic derived artifacts, not another dependency authority"
    - "Unknown/unlocked/unallowed dependencies and unsafe paths fail before mutation"
  artifacts:
    - path: "scripts/generate-capability-plugin.py"
      provides: "Deterministic external component materialization during capability generation"
    - path: "tests/test_capability_plugins.py"
      provides: "Positive and fail-closed coverage for the component contract"
  key_links:
    - from: "plugins/design-engineering/distribution.config.json"
      to: "dependencies/external-skills/apm.lock.yaml"
      via: "external component declaration resolved only against immutable lock state"
---
<objective>
Extend the existing capability publisher/generator so an Agent Plugin can embed selected external Agent Skills while APM remains the single resolution/integrity authority.

Purpose: enable reusable specialist skills without manual vendoring or duplicate lock files.
Output: schema/contract, materializer, transactional publisher support, tests and policy integration.
</objective>
<context>
@.planning/PROJECT.md
@skills/skill-publish/SKILL.md
@scripts/generate-capability-plugin.py
@dependencies/external-skills/apm.lock.yaml
@dependencies/external-skills/apm-policy.yml
</context>
<tasks>
<task type="auto">
  <name>Define the external component declaration contract</name>
  <files>plugins/design-engineering/distribution.config.json, skills/skill-publish/SKILL.md</files>
  <read_first>current capability config schema/validator, #44 ownership modes, APM lock format</read_first>
  <action>Add a minimal repository-owned declaration that names an APM dependency and target skill path. Define canonical/derived boundaries, collision rules, license/provenance requirements, and whether a dependency is consumed-only or intentionally embedded.</action>
  <verify>Config normalization accepts the documented valid case and rejects missing/ambiguous ownership.</verify>
  <acceptance_criteria>No second lock, independent version field, or runtime fetch of mutable upstream instructions is introduced.</acceptance_criteria>
  <done>The contract is explicit enough for deterministic generation.</done>
</task>
<task type="auto">
  <name>Implement lock-backed materialization transactionally</name>
  <files>scripts/generate-capability-plugin.py, skills/skill-publish/scripts/catalog_capability.py</files>
  <read_first>materialize-apm-mirror.py patterns, skill-publish transaction/rollback code</read_first>
  <action>Resolve only the exact committed lock payload, copy the declared skill subtree into the capability's generated/canonical package surface according to ownership rules, preserve attribution/license notices, detect collisions and symlinks/path traversal, and support check/no-op drift mode.</action>
  <verify>Two runs converge; moving a human tag without lock update, missing dependency, changed hash, unsafe path or name collision fails closed.</verify>
  <acceptance_criteria>Failed generation/application leaves no partial capability registration.</acceptance_criteria>
  <done>External components can be materialized reproducibly from APM state.</done>
</task>
<task type="auto">
  <name>Add security and regression tests</name>
  <files>tests/test_capability_plugins.py, tests/test_skill_publish_capability.py, dependencies/external-skills/apm-policy.yml</files>
  <read_first>existing capability and APM tests</read_first>
  <action>Add positive fixtures plus failures for unlocked/unallowed dependency, hash drift, duplicate skill identity, target escape, symlink source, license/provenance omission and unrelated-file mutation.</action>
  <verify>Unit tests, APM policy/audit, generic capability generation check and catalog validation all pass.</verify>
  <acceptance_criteria>The feature is generic; no Emil/Addy-specific code is needed.</acceptance_criteria>
  <done>EXT-01 is complete.</done>
</task>
</tasks>
