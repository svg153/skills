---
phase: 11-figma-mcp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/design-engineering/distribution.config.json
  - plugins/design-engineering/mcp.json
  - plugins/design-engineering/docs/figma.md
  - plugins/design-engineering/evidence/figma-runtime.json
autonomous: false
requirements: ["FIGMA-01"]
user_setup:
  - step: "Authenticate the supported client to the official Figma MCP with a non-sensitive test/design file when runtime evidence is collected."
    why: "Credentials/OAuth remain client-managed and cannot be supplied by repository automation."
must_haves:
  truths:
    - "Figma is an optional official MCP composition, not a hard dependency"
    - "Authenticated read evidence is captured without committing credentials"
    - "Unavailable Figma falls back to repository DESIGN.md/tokens/components"
  artifacts:
    - path: "plugins/design-engineering/distribution.config.json"
      provides: "Canonical Figma MCP provenance/config input"
    - path: "plugins/design-engineering/evidence/figma-runtime.json"
      provides: "Sanitized authenticated runtime evidence"
  key_links:
    - from: "plugins/design-engineering/distribution.config.json"
      to: "plugins/design-engineering/mcp.json"
      via: "existing generated MCP composition"
---
<objective>
Add Figma structured design context as an optional official integration after the baseline is proven.
</objective>
<context>
@plugins/planning/distribution.config.json
@plugins/design-engineering/skills/design-engineering/references/design-md.md
@plugins/design-engineering/docs/pilot.md
</context>
<tasks>
<task type="auto">
  <name>Re-verify official Figma MCP contract and add provenance</name>
  <files>plugins/design-engineering/distribution.config.json, plugins/design-engineering/mcp.json, plugins/design-engineering/docs/figma.md</files>
  <read_first>current official Figma MCP documentation, repository MCP security/composition rules</read_first>
  <action>Confirm current official endpoint/transport/client compatibility. Add only HTTPS/approved config and provenance metadata; document scopes, client-managed auth, design-context precedence and degradation. Generate mcp.json rather than hand-maintain it.</action>
  <verify>Generic MCP schema/security validation passes and no credential-bearing header exists.</verify>
  <acceptance_criteria>No compatibility claim exceeds conformance/discovery evidence collected so far.</acceptance_criteria>
  <done>Portable optional composition is valid.</done>
</task>
<task type="checkpoint:human-action">
  <name>Collect authenticated read evidence</name>
  <action>Authenticate in a trusted interactive supported client, read a non-sensitive Figma design context relevant to the workflow, and capture sanitized evidence distinguishing config discovery from authenticated tool success.</action>
  <verify>Evidence contains provider/client/version, successful read capability, sanitized returned identifiers and no token/cookie/secret material.</verify>
  <done>Authenticated runtime evidence exists.</done>
</task>
<task type="auto">
  <name>Prove degraded operation and DESIGN.md coexistence</name>
  <files>plugins/design-engineering/evidence/figma-runtime.json, plugins/design-engineering/docs/figma.md</files>
  <read_first>design-md precedence rules, Figma evidence</read_first>
  <action>Run a fixture/pilot slice with Figma available and unavailable. Verify Figma enriches structured context but does not overwrite project-owned design memory automatically and absence does not block core work.</action>
  <verify>Both paths pass relevant evals and documentation states exactly what is authoritative.</verify>
  <acceptance_criteria>No second design-system source of truth is created.</acceptance_criteria>
  <done>FIGMA-01 is complete.</done>
</task>
</tasks>
